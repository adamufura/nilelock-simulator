# Backend API (no trailing slash). Override for local dev:
# VITE_API_URL=http://localhost:4000
VITE_API_URL=https://furacomp-products-nilelock-backend.o9oxxq.easypanel.host

# CORS is configured on the backend (CORS_ORIGIN / CORS_EASYPANEL_SUFFIX), not here.
